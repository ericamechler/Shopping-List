export const ListItem = () => {
  return <div>ListItem</div>;
};

// A component that represents a single item in the shopping list.
// It can be rendered within ListDetailsPage and display the item name, quantity, and actions to edit or remove it.
